import ContactSection from "@/components/ContactSection"; // Adjust path based on where you saved the component

export default function ContactPage() {
  return (
    <main className="min-h-screen bg-[#12161F] flex flex-col justify-center">
      {/* You can add a navbar or header here if needed */}
      
      <ContactSection />
      
      {/* You can add a footer or other sections here if needed */}
    </main>
  );
}